These images are **design placeholders**, generated for the README. They are not screenshots of a running `Shore.app`.

Capture the real island, Dock Cove, and Settings window on a Mac after `xcodebuild` and replace these files in-place so the README paths stay valid.
