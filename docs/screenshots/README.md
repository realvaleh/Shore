These images are **design placeholders**, generated for the README from the same housing-neck metrics as `IslandMetrics`. They are not screenshots of a running `Shore.app`.

Capture the real island (compact capsule + expanded player), the drag-time file basket, and the Settings window on a Mac after `xcodebuild` and replace these files in-place so the README paths stay valid.
